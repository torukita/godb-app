## memo

```
$ ./godb-app --help
Usage:
   [flags]
   [command]

Available Commands:
  cleanup     drop table and ...
  dump        Dump table
  help        Help about any command
  prepare     prepare table and ...
  reset       Truncate table in sampledb
  run         Run insert table

Flags:
  -c, --config string      config file (default "./dbconf.yml")
  -e, --env string         environment in config file (default "development")
  -h, --help               help for this command
      --log-level string   panic|fatal|error|warn|info|debug|trace (default "info")
      --version            version for this command

Use " [command] --help" for more information about a command.
```


